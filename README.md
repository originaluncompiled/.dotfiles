# Dotfiles

Personal dotfiles for my development setup.

Using `stow` for selectively choosing which one to use (and keeping them synced/the same across devices)

- [Neovim config shortcuts](./nvim/.config/nvim/README.md)
